# Tech-debt / cleanup audit — 2026-09-05

Audit tip: `6ce0f5f` (`#110` notifications LIVE) on `claude/init-evolved-pros-platform-Q2oUw`.
Sibling: open PR `#111` Home 3-band (`cursor/member-home-3band-wig-7bd1`, mergeable, extra commit `3c6af60`).
In flight: Footer logo realign agent (`bc-0961271a-edf7-432f-b4e7-821d2d6985af`) — do not touch footer logo CSS.
Do not merge. Pricing stays `$49` / `$249`. No member-wall punches. Home IA not rewritten.

This cleanup PR lands only S-sized P1 items marked **FIXED HERE**. Everything else is report-only.

---

## Collision / sibling notes

| Work | State | Collision risk |
| --- | --- | --- |
| `#111` Home 3-band | Open, `MERGEABLE` vs `6ce0f5f` | Overlaps `apps/web/app/(member)/home/page.tsx` + new band files. This PR does **not** touch those paths. After `#111` merges, WelcomeBanner / AccountabilityHub / TodaysEvolution become unused on Home — delete in a follow-up, not now (would break current Home). |
| `#111` footer | Mounts `PublicFooter` on **member Home only** | Partial fix of the member-footer gap. Do not add footer to `(member)/layout.tsx` while `#111` and the logo-realign agent are in flight. |
| Footer logo realign | Running sibling agent, no branch yet | Do not edit `.ep-public-footer-logo*` / brand CSS. |
| `#103` Juan still | Open, unmerged | Binary swap of `juan-fernandez.jpg`. Flag only here. Hash on `#103` (`f9246515…`) does **not** match the “correct `723893…`” note — human must pick the still. |
| Railway `cronSchedule` | Null on `web` | Confirmed in `railway.toml` (no `[deploy] cron` / `cronSchedule`). GitHub Actions is the only in-repo scheduler. Railway MCP was unavailable this run, so dashboard null is taken from the task brief + missing field in `railway.toml`. |

---

## P0 — ship-blocker / security

### P0-1. Live Ep 010 Juan still is still George

| | |
| --- | --- |
| **Path** | `apps/web/public/podcast/guests/juan-fernandez.jpg` |
| **Symptom** | File on Q2oUw tip is JPEG 1290×1004, 994234 bytes, MD5 `3fa241aab6fee6e741208235f1d0fd86` (George NYC-suit face). Live `/podcast/guests/juan-fernandez.jpg` matches. Repo code + migration `083` already point at this path. |
| **Likely cause** | Wrong binary committed; `#100` / `#083` wired the path, not the face. |
| **Size** | S (binary replace) — **owned by open `#103`**. Do not invent bytes here. |
| **Follow-up enough?** | Yes: merge `#103` only after a human confirms the replacement MD5 (PR claims `f92465157088bd8112184a8f3a4fa5bc`; task note says correct is `723893…`). `#111` keeps Juan on the same public path, so it inherits whichever binary is on the branch. |

### P0-2. `#106/#107` leftover: `weekly_leaderboard()` still SECURITY DEFINER

| | |
| --- | --- |
| **Path** | `supabase/migrations/050_weekly_leaderboard_rpc.sql` (not re-locked in `081`/`084`/`085`) |
| **Symptom** | Definer RPC granted to `anon`/`authenticated` can read `users` columns that `#084/#085` revoked from table grants. |
| **Likely cause** | RLS sweep closed table/column grants; older definer RPCs were left as-is. |
| **Size** | M — rewrite to invoker / `member_directory`, or lock EXECUTE to `service_role`. |
| **Follow-up enough?** | Yes, after a prod `pg_proc` / grants dump. Do not guess the live signature in a drive-by migration. |

### P0-3. `vendasta_webhooks` (and possibly `media_stories`) RLS unverified

| | |
| --- | --- |
| **Path** | `supabase/migrations/001_initial_schema.sql`, `035_vendasta_account_mapping.sql`; `media_stories` has no CREATE in `supabase/migrations/` (only `086` UPDATEs) |
| **Symptom** | `vendasta_webhooks` sits in `public` without an RLS migration. `media_stories` exists in generated types but has no repo `ENABLE RLS`. |
| **Likely cause** | Out-of-tree / prod-only DDL. `#107` comment already admits 084/085 drift. |
| **Size** | M — prod audit + migration. |
| **Follow-up enough?** | Yes. **Do not apply a speculative RLS migration from this audit.** |

**No new XSS P0.** `#106` sanitizers (`apps/web/lib/security/html.ts` + tests) cover pinned + Media `dangerouslySetInnerHTML`. Notification bodies render as React text / `**bold**` only.

---

## P1 — this week

### P1-1. `#110` cron routes exist; scheduler never called them **FIXED HERE**

| | |
| --- | --- |
| **Path** | `apps/web/app/api/cron/member-nudges/route.ts`, `apps/web/app/api/cron/goal-snapshots/route.ts`, `.github/workflows/cron.yml`, `railway.toml` |
| **Symptom** | `#110` shipped `GET /api/cron/member-nudges` (hourly-safe; daily no-ops before 17:00 UTC) and hooked weekly WIG check-ins onto `goal-snapshots`. GitHub `cron.yml` only hit expire-tiers / renewal / event-reminders / daily-digest at 08:00 UTC. Railway `web` has no `cronSchedule`. Evening progress + weekly WIG bells therefore only fire if a member opens Home (`enqueueSessionNudges`). |
| **Likely cause** | `#110` documented “point Railway/cron-job.org at the route” but did not update the in-repo scheduler. Default branch **is** Q2oUw, so GitHub Actions cron is the live scheduler. |
| **Size** | S |
| **Follow-up enough?** | Wiring is in this PR: 17:00 UTC `member-nudges`, Monday 08:15 UTC `goal-snapshots`, existing 08:00 jobs gated so the evening tick does not double-send digest/expiry. Remaining: confirm `APP_URL` + `CRON_SECRET` GitHub secrets; other unschedulable routes (`podcast-sync`, `publish-posts`, `warmup`) stay P2. |

### P1-2. `#110` member-facing copy still uses em dashes **FIXED HERE**

| | |
| --- | --- |
| **Path** | `apps/web/lib/notifications/intents.ts` (`wigCopy('weekly')`), `apps/web/lib/notifications/create.ts` (`notifyReply`), `apps/web/lib/resend/emails/DigestEmail.tsx` (preview) |
| **Symptom** | `#108` banned em dashes on Media chrome; `#110` reintroduced them in bell/email copy. |
| **Likely cause** | Comment-style punctuation leaked into user strings. |
| **Size** | S |
| **Follow-up enough?** | Yes. This PR rewrites those three strings and locks `#110` copy helpers. Comments may keep em dashes. |

### P1-3. Missing tests for cron schedule / intent copy **FIXED HERE**

| | |
| --- | --- |
| **Path** | `apps/web/lib/notifications/intents.test.ts` (exists, decisions only), no cron test before this PR |
| **Symptom** | Intent math was tested; the scheduler file and AI-tell copy were not. A future cron.yml edit could drop `#110` again. |
| **Likely cause** | `#110` shipped route + decision tests, not an ops lock. |
| **Size** | S |
| **Follow-up enough?** | This PR adds `cron-schedule.test.ts` + em-dash assertions. A true HTTP cron route test still needs a follow-up (auth + adminClient). |

### P1-4. `#110` filter badge hardcodes `#ef0e30` **FIXED HERE**

| | |
| --- | --- |
| **Path** | `apps/web/components/notifications/NotifFilter.tsx` |
| **Symptom** | Count pill ignores `--red` / `--notif-*` tokens. Light+dark already tokenized on the rest of the filter. |
| **Likely cause** | Leftover raw brand hex from the filter rewrite. |
| **Size** | S |
| **Follow-up enough?** | Yes. |

### P1-5. Media article OG/JSON-LD fallback 404s **FIXED HERE**

| | |
| --- | --- |
| **Path** | `apps/web/app/(public)/media/[pillar]/[slug]/page.tsx` → `/og-default.png` (file is **not** in `apps/web/public/`) |
| **Symptom** | Stories without `featured_image_url` advertise a missing image. No `msp-og` string exists in this repo (nothing-today for that name). |
| **Likely cause** | Placeholder path never committed. |
| **Size** | S |
| **Follow-up enough?** | This PR falls back to the existing navy wordmark (`DEFAULT_OG_IMAGE`). A real 1200×630 share card is a later sprint. |

### P1-6. Footer missing on member routes (except `#111` Home)

| | |
| --- | --- |
| **Path** | `apps/web/app/(member)/layout.tsx` (no footer). `#111` mounts `PublicFooter` only in `home/page.tsx`. |
| **Symptom** | `/academy`, `/community`, `/notifications`, `/messages`, `/events`, `/profile`, `/settings`, `/leaderboard` have no Terms/Privacy/Contact. Public shell hides footer when `.ep-member-shell` is nested (logged-in `/podcast`). |
| **Likely cause** | Footer-1 scoped `PublicFooter` to the marketing shell. |
| **Size** | M — layout mount + scroll/safe-area, **after** logo realign + `#111`. |
| **Follow-up enough?** | Yes. Do not land a layout footer in this PR. |

### P1-7. Theme one-offs still break light mode on member surfaces

| | |
| --- | --- |
| **Path** | `apps/web/app/(member)/messages/MessagesClient.tsx`, `ConversationThread.tsx`, `leaderboard/page.tsx`, `(member)/**/error.tsx`, `academy/[pillarSlug]/[lessonSlug]/page.tsx`, `WelcomeBanner.tsx` / `AccountabilityHub.tsx` (current Home) |
| **Symptom** | Hard `#0A0F18` / `#ef0e30` / white rgba palettes ignore `--bg-page` / `--text-primary`. Media hub is **intentionally** light-locked (`colorScheme: 'light'`) — leave that. |
| **Likely cause** | Pre-token cinematic / chat UIs. `#109` only shipped stack utilities. |
| **Size** | L (member shell), M (messages/leaderboard) |
| **Follow-up enough?** | Yes. `#111` bands already use semantic tokens; do not restyle those here. |

### P1-8. `#109` rhythm bypassed on recently touched notification chrome

| | |
| --- | --- |
| **Path** | `apps/web/app/(member)/notifications/NotificationsContent.tsx` (`space-y-2`), `NotifFilter.tsx` (`py-[9px]`) |
| **Symptom** | Intra-list gaps are fine; page stack is not on `ep-stack` / 16/24/32/40. Home/academy/community already use `.ep-stack`. |
| **Likely cause** | `#110` did not adopt `#109` utilities on the feed page. |
| **Size** | S–M |
| **Follow-up enough?** | Yes. Not changed here (touches notification layout, not a ship bug). |

---

## P2 — backlog

| ID | Path | Symptom | Cause | Size | Follow-up? |
| --- | --- | --- | --- | --- | --- |
| P2-1 | `apps/web/components/sponsors/*` (8 files) | Entire pre-refactor sponsor-card tree has zero importers | Replaced by `HomeSponsorAd` / `IabImageAd` | S | Yes — delete directory |
| P2-2 | `HomeSponsorRow.tsx`, `HomeSponsorAdClient.tsx`, `HomeSponsorAd.tsx` `HomeSponsorRow` / `SponsoredEyebrow` exports | Dead shims | Ads refactor leftover | S | Yes |
| P2-3 | `PillarJourneyStrip.tsx`, `HomeMetricsStrip.tsx`, `ScoreboardHero.tsx`, `lib/scoreboard/fetchPillarProgress.ts` | Unused | Home scoreboard churn | S | Yes — **after `#111`**, also WelcomeBanner / AccountabilityHub / TodaysEvolution / ClimbingToward if Home no longer mounts them |
| P2-4 | `DailyPulseCard.tsx` component body; `PodcastReelTile.tsx` component body | Components unused; **types still live** | Extract types first | M | Yes |
| P2-5 | `AccountabilityHub` `variant="full"` | Never passed | Legacy scoreboard branch | M | Yes |
| P2-6 | `.ep-rule`, `.ep-ad-slot--lean`, Tailwind `ep-stack-lg` | CSS utilities unused | `#109` shipped extras | S | Yes |
| P2-7 | `scripts/generate-media-desk-copy-sql.mjs`, `MEDIA_PAPER` | One-off / unused export | `#108` generator | S | Yes |
| P2-8 | `nudges.ts` `maybeNudgeWigForUser` / `maybeNudgeDailyForUser`, `fanoutContentDrop`, `createNotificationIfFresh`, `INTENT_TYPE.daily` | Exported, unused externally | Tightening API surface | S | Yes |
| P2-9 | `api/cron/podcast-sync`, `publish-posts`, `warmup` | Not in `cron.yml`; Railway schedule null | External / admin-button assumed | S | Document + decide owner |
| P2-10 | Cron auth duplicated 9 ways (Bearer-only vs Bearer **or** `x-cron-secret`) | `#110` dual-auth on two routes only | Extract helper | S | Yes |
| P2-11 | Fan-out / admin broadcast / speaking inquiry insert **around** `createNotification` | Several insert paths, one table | Intentional perf; not a second engine | M | Optional converge |
| P2-12 | Member routes inherit marketing OG; no `robots: noindex` | Shared member URLs can look like the public homepage | Layout metadata only sets title | M | Yes |
| P2-13 | `docs/LAUNCH_PLAN.md` still lists `$79` VIP | Stale launch checklist | Pre-$49 ladder | S | Docs only |
| P2-14 | `TODO(VENDASTA-3/4)` in `renewal-reminders`, `admin/revenue` | Dead/unwired billing todos | Out of scope (billing) | — | Leave |
| P2-15 | `#111` leftover comments still say “WelcomeBanner” after the fold rewrite | Comment drift | Copy-paste from current Home | S | Fix on `#111` or post-merge |
| P2-16 | `#111` Home-only footer vs later layout footer | Double footer if both land | Parallel agents | S | Coordinate |
| P2-17 | `084` comment “production may have drifted” | Repo/prod policy names may still differ | `#107` landed 085 to match prod | M | `pg_policies` dump |

---

## Nothing-today (clean in this audit)

- **Pricing / walls:** `$49` / `$249` locked in `lib/pricing.ts` + conversion tests. No member-wall punches found in recent diffs.
- **Duplicate notification engine:** Single `createNotification`. Session + hourly + weekly + publish fan-out share dedupe. Not a second service.
- **`msp-og`:** No symbol, file, or route by that name. Closest wound was the missing `/og-default.png` fallback (P1-5).
- **Migration 080–086 duplicate column adds:** None. `086` is copy-only. `083` is the Juan URL write (binary still wrong — P0-1).
- **XSS sinks in notifications / community feed bodies:** Closed by `#106` + React text.
- **Home IA:** Not redesigned here. `#111` is the band lock; this PR does not rewrite bands.
- **GSC, clips/Social, new bots/clocks, billing, Media masthead:** Untouched.
- **`#109` 16/24/32/40 token scale:** Present in `packages/ui/tokens` (`spacing[4/6/8/10]`) and `.ep-stack` on Home/academy/community. Bypass is limited (P1-8 / P2-6), not a missing system.
- **`#108` Media markdown AI-tell:** `copyHygiene.test.ts` still locks em dashes + Bottom Line headers on `content/media/*.md` and `086`.

---

## `#111` debt (read-only; do not redesign bands)

- Mergeable on `6ce0f5f`. Extra commit `3c6af60` stops pinning stale LIVE events — good, not a conflict with this PR.
- Uses `ep-stack` / `ep-page-gutter`, semantic tokens, `enqueueSessionNudges`, `allowedEpisodeStillUrl` for Juan. No new notif engine.
- Footer only on Home (P1-6 / P2-16).
- Leaves `WelcomeBanner.tsx` in-tree “for other surfaces” — after merge it is Home-dead (P2-3).
- Juan helper still serves the George binary until `#103` (or a confirmed still) lands.
- Do not rewrite the three bands in a cleanup PR.

---

## Recommended next sprints (not this PR)

1. Human-approve Juan still (`#103` vs `723893…`) and merge.
2. Member footer in layout **after** logo realign + `#111`.
3. Prod RLS dump for `weekly_leaderboard`, `vendasta_webhooks`, `media_stories`.
4. Tokenize messages + leaderboard + member error pages.
5. Delete dead sponsor/home/scoreboard files **after** `#111` merges.
