# Evolved Pros Fit (2026-09-12)

George said go. Not LIVE. Merge is George/CoS ASK-ALWAYS.

## Product locks

- Fit sits behind VIP $49. Community sees tease + Upgrade to VIP only.
- Instructional video guides. Watch on phone, iPad, laptop, or cast to TV.
- Move codes are FO55-### (letter O). Never F055 / F005.
- No em dashes in Fit copy.
- Mastheads use v4 PNG lockups only (equal letter height). Fit: EVOLVED PR + barbell disc + S FIT. Media: EVOLVED + megaphone disc + MEDIA (no Pros). Podcast LogoMark unchanged. Never typed EVOLVED PROS text.
- 55+ hip-aware. HIP MOD chips name the modification.
- `/admin/fit` is light-only on the #145 Kajabi admin shell.

## Runtime data

`apps/web/lib/fit/moves.ts` is the source of truth until the SQL below is applied. Admin Sync library reloads those fixtures. Persistence + Mux ids wait on this migration.

## SQL

See `supabase/migrations/092_fit_moves.sql`.

Thin `fit_moves` table: code, slug, title, focus, location (Hotel/Studio), status (published/pilot/draft), featured, hip_mod, hip_mod_note, reps, duration, required_tier default `vip`. CHECK enforces `^FO55-[0-9]{3}$`.

Do not apply on a LIVE claim. Apply when George wants CMS writes.

## Sample move

Wall-supported RDL · FO55-035 · HIP MOD soft hinge, no deep chase · Hotel · published.
