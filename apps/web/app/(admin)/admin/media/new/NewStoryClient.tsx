'use client'

import { MediaStoryForm } from '../MediaStoryForm'

// Manual story creation only. The AI writer (StoryAIWriter) was removed for
// launch — see Sprint 4. Restore from git history to bring it back.
export function NewStoryClient() {
  return <MediaStoryForm />
}
