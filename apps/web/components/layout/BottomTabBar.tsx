'use client'

// Stub — full implementation delivered by Sprint Mobile agent.
interface BottomTabBarProps {
  role: string | null
  unreadCount: number
  dmUnreadCount?: number
}

export function BottomTabBar({ role: _role, unreadCount: _u, dmUnreadCount: _d }: BottomTabBarProps) {
  return null
}
